# Worklog — TEL Telemetry: config marking + per-opposition events

- **Date:** 2026-06-19
- **Subagent:** qa-playtest-coordinator
- **Milestone:** M1.1
- **Branch:** qa-playtest-coordinator/TEL
- **Commit:** 66ec131480f100c9b3ab0070b9d59f1427ee6526 (worklog SHA self-reference fixed in a
  follow-up commit; the code + test diff landed in `66ec131`'s tree).

## What changed
Implemented TEL (M1.1 Wave 1), the telemetry contract for the cost-axis experiment:
1. **Config-marking** — `run_started` now snapshots the active `RunConfig.to_flat_dict()`
   onto `data.run_config` (additive payload, NO schema bump) so every run is a labelled
   experiment. All-off config reproduces the M1.0 baseline as the in-build control.
2. **Per-opposition rows** — pre-declared the 11 remaining M1.1 EventBus signals (TEL is
   sole `event_bus.gd` editor for the milestone) and wired the 7 opposition telemetry
   listeners (R1–R4 awoke/caught/return-cost/exposure-crossed/penalty/branch/lost-proxy)
   into Telemetry with primitives-only JSONL payloads.

## Files touched
- `systems/event_bus.gd` — added the 11 M1.1 signals (the 7 opposition telemetry signals +
  R3's 4 penalty/meter signals) in one block. **Did NOT re-declare `depth_changed`** — it
  was already pre-declared on `main` (commit on line 81); a second declaration is a GDScript
  parse error. Confirmed exactly one `signal depth_changed` remains.
- `systems/telemetry/telemetry_schema.gd` — added the 7 `EventType` string constants
  (`HAZARD_AWOKE`…`NAV_LOST_PROXY`) and appended them to `ALL_TYPES`. **No** `SCHEMA_VERSION`
  bump (stays 1); **no** `ENVELOPE_KEYS` change.
- `systems/telemetry/telemetry.gd` — wired the `run_config` snapshot into `_on_run_started`;
  added `_active_run_config_dict()` (reads `/root/GameState`.active_run_config, `{}` if null)
  and `_elapsed_ms()`; connected + handled the 7 new signals per spec §5b. TEL stamps
  `run_t_ms` itself via `_elapsed_ms()` on `hazard_caught` / `exposure_crossed` and flushes
  after those high-value rows. No `depth_changed` listener / `DEPTH_CHANGED` row (ratified §4 /
  Decision 2 — declined; reuse existing `band_depth_reached`).
- `tests/test_telemetry_config_marking.gd` + `.tscn` (new) — scene-based headless test (live
  autoloads, per M1_As_Built testing constraint). Asserts: run_started carries
  `data.run_config` with all flat keys + reflected R1-on values; each of the 7 opposition row
  types is in `ALL_TYPES`, appears in the log when emitted, carries the envelope keys and a
  primitives-only payload; envelope `v == 1`; `ENVELOPE_KEYS` literal unchanged; run_ended
  M1.0 data shape unchanged; TEL stamped its own `run_t_ms`. Prints `TEL CONFIG MARKING OK`.

## The 11 signals added (depth_changed NOT among them)
`hazard_awoke`, `hazard_caught`, `return_cost_incurred`, `exposure_crossed`,
`exposure_penalty`, `nav_branch_taken`, `nav_lost_proxy`, `exposure_speed_mult_changed`,
`exposure_vision_mult_changed`, `exposure_clock_tax`, `exposure_meter_changed`.

## Checks run
- [x] `godot --headless --import` clean — exit 0, no parse errors (confirms no duplicate
  `depth_changed`).
- [x] `godot --headless --script res://tools/ci_smoke_test.gd` → `SMOKE OK — M0 architecture
  spike healthy`, exit 0.
- [x] `godot --headless res://tests/test_telemetry_config_marking.tscn` → `TEL CONFIG MARKING
  OK` (9 rows checked), exit 0.
- [x] `bash tools/run_gdunit.sh` → `30 test cases | 0 errors | 0 failures`, Exit code 0,
  `GdUnit4 run PASSED`. Count unchanged at 30 (the new test is a scene-harness test, not a
  GdUnit `_test_*` suite — same pattern as the existing `test_telemetry_jsonl.tscn`).
- [x] Definition of done met: objective checks (config snapshot, 7 row types, envelope/arity
  invariants) automated and green; determinism unaffected; `run_ended` arity unchanged
  `(reason, duration_s, depth_reached)`; smoke + GdUnit gates green.

## Design deviations
none. Implemented exactly per `TEL_telemetry_config_marking.md` §2–§6 and the §8 ratified
decisions: no schema bump, no new envelope key, no `depth_changed` re-declaration, no
`depth_changed` TEL row, TEL self-stamps `run_t_ms`, log-every-event (no sampler), and the
locked `run_ended` arity preserved (new end-causes reuse `run_ended.reason`).

## Handoffs / follow-ups
- Wave-2 R1–R4 only EMIT the now-declared signals — they must NOT touch `event_bus.gd`.
- The `nav_lost_proxy` metric string is locked to `"time_no_depth_progress"` (Decision 3);
  R4 emits `StringName &"time_no_depth_progress"`.
- BUG2 emits `depth_changed` from `game_state.gd` (already declared); TEL did not add a
  per-depth-change row (Decision 2).
- Did NOT touch `systems/game_state.gd`, `scenes/game/main_game.gd`, or any opposition system.
