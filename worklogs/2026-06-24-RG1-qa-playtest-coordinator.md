# Worklog — RG1 M1.5 playtest build + verify + changelog

- **Date:** 2026-06-24
- **Subagent:** qa-playtest-coordinator
- **Milestone:** M1.5 (Agency & Legibility)
- **Branch:** qa/RG1-m15-verify
- **Commit:** <set below>   ← the RG1 verify+doc+changelog commit

## What changed

RG1 is the M1.5 re-gate VERIFY + DOCUMENT + PUBLISH capstone (not new gameplay — L0–L5
were already built + merged on `main`). This task:
1. Authored the headless verify driver `tests/test_rg1_m15_verify.gd` (+ `.tscn` + `.gd.uid`),
   mirroring `test_rg1_m14_verify` and adapted to the M1.5 levers (throwing L1, room-bound
   pursuer L2, K5 `*_kills` toggles L5). It prints `RG1 M1.5 VERIFY OK` and exits 0.
2. Authored the RG1 doc `design/M1_5_Tasks/RG1_playtest_build.md` (mirrors the M1.4 RG1 doc:
   goal & intent, what's wired, the preset table, the verify matrix [per-feature isolation +
   stacked/baseline + end-cause/telemetry + subjective-deferred], headless-verified-vs-deferred,
   config-sweep guidance for the Director, acceptance criteria).
3. Updated `changelog.txt` for the M1.5 build (delta from M1.4 as a clean feature list:
   throwing, the room-bound slow-patrol pursuer, money-below-timer, context-correct grab
   prompt, the updated F/Q/E/Space input scheme, knob count 81 → 89).

The verify driver confirms (headless): the shipped `make_default_play_preset()` is the full
M1.4 fun stack PLUS `throw_enabled=true` + `r1_spawn_room_only=true` + `r1_patrol_speed=28.0`
(and the K5 `*_kills` stay `true` = lethal); the all-off `RunConfig` band fingerprint is still
byte-identical at `e943ac9c8bc1` and building the preset does NOT move it (the M1.5 levers are
pure run-state, never feed `fingerprint()`); `RunConfig.new()` stays all-off after building the
preset (no leak); `to_flat_dict()` carries all 89 keys incl. the 8 M1.5 knobs; the L1 throw seam
(ThrownItem Area2D layer-0/mask-18 + `_try_throw`/`_spawn_thrown_item`) wires up and a real
ThrownItem flies under BandContainer through the driven seam; the L2 R1 pursuer spawns and learns
its spawn-room bounds; extract + timeout end-causes are reachable with the K5 kills off; the
`run_ended` arity is intact + `duration_s > 0`; every telemetry row carries `v == SCHEMA_VERSION`.

## Files touched

- `tests/test_rg1_m15_verify.gd` — NEW. The M1.5 headless verify driver (12 headless rows).
- `tests/test_rg1_m15_verify.tscn` — NEW. Scene wrapper so the autoloads resolve as live nodes.
- `tests/test_rg1_m15_verify.gd.uid` — NEW. Generated by `--import`.
- `design/M1_5_Tasks/RG1_playtest_build.md` — NEW. The RG1 M1.5 build/verify doc.
- `changelog.txt` — UPDATED. Prepended the M1.5 feature delta (M1.4 section retained below).

## Checks run (full M1.5 verify matrix — all green)

- [x] `godot --headless --import` clean (no parse errors).
- [x] `godot --headless --script res://tools/ci_smoke_test.gd` → **SMOKE OK — M0 architecture spike healthy** (exit 0).
      NOTE: the smoke test is a `--script` MainLoop, NOT a scene — it MUST be run with `--script`
      (running it as a bare scene hangs indefinitely). Only the `.tscn` verify/knob tests run as scenes.
- [x] `godot --headless res://tests/test_run_config.tscn` → **R0 OK** (89 knobs; J1 preset is the F1 stack, trap-free, no leak).
- [x] `godot --headless res://tests/test_config_menu.tscn` → **CONFIG MENU OK** (89/89 knobs bound + reachable; Reset returns all-off).
- [x] `godot --headless res://tests/test_rg1_m15_verify.tscn` → **RG1 M1.5 VERIFY OK** (12 rows headless-verified; 7 deferred; exit 0).
- [x] `godot --headless res://tests/test_throw_mechanic.tscn` → **L1 OK** (hazard-hit kills+consumes+throw_killed_hazard; miss → throw_missed+junk_dropped; one-shot `_spent`).
- [x] `godot --headless res://tests/test_pursuing_hazard.tscn` → **PURSUING HAZARD OK** (incl. L2 room-bound chase in-room / slow-patrol out-of-room / chase-everywhere fallback / idle-pivot at patrol_speed 0).
- [x] `godot --headless res://tests/test_rg1_m14_verify.tscn` → **RG1 M1.4 VERIFY OK** (no regression).
- [x] `godot --headless res://tests/test_new_hazard_spawn.tscn` → **K5i OK** (no regression).
- [x] `godot --headless res://tests/test_bandgen_determinism.tscn` → **BUG4 BRANCH-RATE-INDEPENDENT SEAL OK** (no regression).
- [x] All-off band fingerprint confirmed `e943ac9c8bc1` (asserted by the M1.5 verify driver + R0/M14).
- [x] Definition of done met: "the headless verify test prints `RG1 M1.5 VERIFY OK` and exits 0" — yes.

## Design deviations

**none.** The verify driver, doc, and changelog are on-spec with the RG1 row in `M1.5_Breakdown.md`
§"Phase 3 Dispositions & Phase 4 Lock". The preset values asserted (`throw_enabled=true`,
`r1_spawn_room_only=true`, `r1_patrol_speed=28.0`, knob count 89, fp `e943ac9c8bc1`, K5 `*_kills`
default `true`) match the locked contract. Two implementation notes (not design deviations):
- The static throw-seam method check uses `GDScript.get_script_method_list()` (not `has_method()`
  on the script resource, which does not report instance methods) — a test-internal choice.
- The L1 throw-assembled row spawned a real ThrownItem headlessly (the seam fully fired); the
  felt throw/kill/highlight remain on the manual checklist per spec.

## Handoffs / follow-ups

- **Itch publish** (standing playtest-gate step): outcome recorded in this worklog's commit
  message / the RG1 final report (export OK vs. butler push network-gated).
- RG2 (telemetry analysis vs M1.0–M1.4) + RG3 (Director verdict) follow after the human playtest.
- Orchestrator to integrate this branch into `main` (do NOT self-merge per the RG1 contract).
- Environment note for future QA runs: stale backgrounded `godot` instances deadlock on the
  import lock (memory: "never run two godot instances concurrently"). Run each godot invocation
  to completion before the next; the smoke test needs `--script`.
