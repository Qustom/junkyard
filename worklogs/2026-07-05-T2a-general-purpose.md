# Worklog — T2a New opposition #5: Ambusher (def + one Concealment component)

- **Date:** 2026-07-05
- **Subagent:** general-purpose (programmer)
- **Milestone:** M1.10 (Wave 1)
- **Branch:** worktree-agent-a19da54ac4b53e7e8
- **Commit:** 0c72f686debbff00f6bcdad213d9f60e025bc3f0 (code + test); worklog SHA fix in the follow-up commit

## What changed
Added the AMBUSHER opposition ("The Lurker") as the TG3 scalability proof: `ambusher.tres`
(data) + ONE new `Concealment` component + an honest per-def Actor-host shell + scene + test.
The pounce is the REUSED S6a `ChargeLane` verbatim (HIDDEN→ARMED→POUNCE→EXPOSED ≡
DORMANT→TELEGRAPH→CHARGE→RECOVER); arm = reused `ProximityTrigger`, kill = reused
`LethalContact(&"external")`, tells = reused `TelegraphFSM`, throw-death = reused
`ThrowInteraction`. `Concealment` owns the only new axis: near-invisibility at rest, a faint
floor-tell decal, the un-hittable gate held as BOTH `collision_layer = 0` AND out of the
`"hazard"` group (OQ-10 amendment 2 — clean pass-through, not group-only), and the one-shot
SPENT husk — driven off `ChargeLane.on_state_changed`, never a ticker. One-shot is host
orchestration (`_spent` stops re-ticking `ChargeLane`); `re_hide_s` maps to `cooldown_s` for
the ships-dormant re-hide variant. Ships OFF-default (`min_band=3`, not in any lever/deck),
so the all-off fingerprint is byte-identical.

## Files touched (all NEW — zero shared-file edits)
- `Game/data/oppositions/ambusher.tres` — the def: id `&"ambusher"`, card (min_band 3,
  credit_cost 2, per_room_cap 2, per_band_cap 6), 12-key `params` + bijective `param_schema`
  (one `trap_if_neutral` on `lunge_speed`), lethal/kills=true.
- `Game/scenes/hazards/components/concealment.gd` — the ONE new component.
- `Game/scenes/hazards/ambusher_hazard.gd` — the honest per-def Actor host (S6a Wave-4
  amendment cost); maps Ambusher-semantic params → ChargeLane keys, orchestrates one-shot.
- `Game/scenes/hazards/ambusher.tscn` — host shell: root `Ambusher` CharacterBody2D,
  `groups=["hazard"]`, layer 16 / mask 2 at author time; `$Body`/`$Tell`/`$FloorTell` greybox
  Polygon2D + CollisionShape2D.
- `Game/tests/test_ambusher.gd` + `.tscn` — headless scene test (DoD 1,2,a–j).
- (`.uid` sidecars auto-generated by the import.)

## Checks run
- [x] `godot --headless --path Game --import` clean (no parse errors)
- [x] `godot --headless --path Game --script res://tools/ci_smoke_test.gd` → SMOKE OK
- [x] `res://tests/test_opposition_def_schema.tscn` → DEF SCHEMA OK (8 defs; params↔schema
  bijection extends to the Ambusher via the dir-scanning net — NO hard global def-count assert)
- [x] `res://tests/test_ambusher.tscn` → **T2a OK** (all cases green)
- [x] All-off fingerprint **`e943ac9c8bc1` byte-identical** (asserted inside test case 2)

**Definition of done (met):** all-off fp unmoved; params↔schema bijection green via the
dir-scanning net; `test_ambusher` green — HIDDEN non-lethal + un-hittable (layer 0
pass-through) + throw pass-through; arm radius reveal; tell precedes pounce by the lead
(exactly 1 telegraph row); pounce kill `kills`-gated (BUG6 once); EXPOSED throw-kill;
one-shot SPENT (+ re_hide_s re-arm path); deterministic placement (per_band_cap 6 binds,
min_band 3 refuses band-depth-2); RNG-free; tells triangulate; import clean + smoke green.
Tests run as SCENES, one godot instance at a time.

## Deferred CFG_FIELD_* gloss rows (verbatim — for the Wave-1 merge commit into
`Game/ui/config/config_strings.csv`; NOT edited here per the T2a↔T2b collision adjudication)
```
CFG_FIELD_AMBUSHER_BASE_COUNT,Spawn card — base Lurkers per eligible room (deck lane)
CFG_FIELD_AMBUSHER_COUNT_PER_DEPTH,Spawn card — extra Lurkers per depth unit
CFG_FIELD_AMBUSHER_ARM_RADIUS,Arm radius (px) — distance at which a hidden Lurker springs up and telegraphs
CFG_FIELD_AMBUSHER_TELL_LEAD_S,Tell lead (s) — the readable wind-up before the pounce
CFG_FIELD_AMBUSHER_LUNGE_SPEED,Pounce speed (px/s) along the locked lunge lane
CFG_FIELD_AMBUSHER_LUNGE_DIST,Pounce distance (px) before it overshoots and is exposed
CFG_FIELD_AMBUSHER_EXPOSED_WINDOW_S,Exposed (s) after the pounce — the throw-punish window
CFG_FIELD_AMBUSHER_LUNGE_WIDTH,Lunge lane width (px) — the swept lethal corridor
CFG_FIELD_AMBUSHER_CONCEALED_ALPHA,Body visibility at rest (0 = invisible; the floor tell carries the read)
CFG_FIELD_AMBUSHER_TELL_ALPHA,Floor-tell decal visibility while hidden (the "read the room" affordance)
CFG_FIELD_AMBUSHER_RE_HIDE_S,Re-hide delay (s) — 0 = one-shot (spent after the pounce); >0 = re-arm after this delay
CFG_FIELD_AMBUSHER_KILLS,Lethal pounce kills (off = harmless contact for sweeps)
```
(12 rows.)

## Bespoke-code ledger (the TG3 scalability evidence)
| Artefact | Kind | Non-comment code lines | In the "def + one component" budget? |
|---|---|---|---|
| `data/oppositions/ambusher.tres` | data | (1 resource, 12 params) | Yes — the point |
| `scenes/hazards/components/concealment.gd` | **NEW component** | **39** | **Yes — the ONE new component** (incl. ~5 lines for the layer-0 write, OQ-10 amendment) |
| `scenes/hazards/ambusher_hazard.gd` | host shell | 132 | Expected honest per-def Actor host (S6a Wave-4 amendment) |
| `scenes/hazards/ambusher.tscn` | scene | 1 | Expected (host + 3 polygons + collision) |
| `tests/test_ambusher.gd` + `.tscn` | test | (excluded — test code) | — |
| `config_strings.csv` gloss rows | data | (12, deferred to merge) | Yes — data |

**Edits to shared/reused files: ZERO** — `charge_lane.gd`, `proximity_trigger.gd`,
`telegraph_fsm.gd`, `lethal_contact.gd`, `throw_interaction.gd`, `thrown_item.gd`,
`encounter_builder.gd`, `spawn_service.gd`, `event_bus.gd` all untouched. Both §4 risk points
closed as Phase 3 predicted: (1) host `_spent` gating one-shot needed no `ChargeLane` edit;
(2) `throwable_while_charging = true` + `Concealment` sole group/layer owner needed no seam.
Net honest cost of this opposition: one 39-line component + a host shell + data + a test.

## Design deviations
None. Built to the T2a `Resolved Decisions (Phase 3, BINDING)`: reuse `ChargeLane` verbatim
(OQ-1); host `_spent` one-shot (OQ-2); fatal `kills`-gated pounce (OQ-3); defaults
`tell_alpha 0.28`/`concealed_alpha 0`/`tell_lead_s 0.45` (OQ-4); no mimic variant (OQ-5);
standard placement (OQ-6); ARMED throw-killable via reveal group-join (OQ-7); semantic param
keys mapped in the host (OQ-8); band-3-native `min_band 3` (OQ-9); `Concealment` sole owner of
BOTH `collision_layer` (HIDDEN=0, ARMED/POUNCE/EXPOSED=16, SPENT=16) and `"hazard"` group,
with `hide_conceal()` seated LAST in `setup()` to neutralise ChargeLane's bind-time group-add
(OQ-10, both amendments). Trap flag on `lunge_speed`; arm_radius schema min 32 with the 0-gate
exercised only via injected test params.

## Handoffs / follow-ups
- Orchestrator: apply the 12 `CFG_FIELD_AMBUSHER_*` gloss rows above into
  `config_strings.csv` in the single Wave-1 merge commit (alongside T2b's rows).
- Director queue (from Phase 3, unchanged — recommendations attached in the spec): OQ-3
  (fatal vs stagger), OQ-4 (tell subtlety starting values), OQ-5 (mimic variant), OQ-9
  (band-3 exclusivity). TG2 first-encounter deaths drive the OQ-4 sweep.
- `band_three` deck membership + the third hub portal are T3/T4's job — the Ambusher becomes
  reachable only there; nothing wired here beyond `min_band=3`.
