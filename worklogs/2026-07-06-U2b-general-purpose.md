# Worklog — U2b New opposition #8: Sentry (def + one `LaneWatch` component)

- **Date:** 2026-07-06
- **Subagent:** general-purpose (programmer; also covered the character-animator scope — inline greybox Polygon2D lane strip + bolt + turret body per D-RAT-4, no PixelLab)
- **Milestone:** M1.11 (Wave 1)
- **Branch:** worktree-agent-a8bb30986084b881e (isolated worktree; feature branch for the Wave-1 merge)
- **Commit:** dc1c124c873781df8919873716b4b27999033a75

## What changed

Shipped the Sentry ("Gate-Sentry", id `&"sentry"`) — the at-range / projectile-emission
Phase-E proof — as **data + ONE new component + a thin host**, with **zero shared-file
edits**. `LaneWatch` owns: second-tick lane acquisition (amendment A1 — 8-octant derive
vs the world mask, longest-pick, strict-`>` fixed tie-break; `lane_dir_deg >= 0`
authored override), the latched effective clear length `_lane_len_eff` used by the strip
visual, the crossing test, and the bolt's max travel (A2 — a dense-cover spawn degrades
to a short honest lane, no fallback machinery), the `IDLE→WINDUP→FIRE→COOLDOWN` FSM
(windup entry LOS-gated; the FSM gated on `_acquired`), and the component-owned driven
bolt: a `Polygon2D` advanced down the locked lane with a per-frame script-swept
`kills`-gated lethal test fed to the reused `LethalContact` `&"external"` seam (the
ChargeLane segment idiom) + a world `intersect_ray` stop (wall-blocked, stop-on-first-
hit, no pierce; travel capped at `_lane_len_eff`; `thrown_item` reuse REJECTED per
OQ10). The body never moves, is never contact-lethal, and never leaves the `"hazard"`
group (permanent throw-disable via reused `ThrowInteraction` `&"die"` — DR-4).
`bolt_speed = 0` is trap-neutral (WINDUP hands straight to COOLDOWN — no bolt, no
threat). Pause-safe latch; re-`setup()` re-derives identically (A3). Final spawn card
per breakdown amendment 1 (supersedes the body's cap 4): `credit_cost 2 · per_room_cap
1 · per_band_cap 5 · min_band 4 · cap_group &"new_hazards"`. Ships OFF-default; enters
only `band_four`'s deck (U3).

## Files touched (all NEW — zero edits to existing files)

- `Game/scenes/hazards/components/lane_watch.gd` — the ONE new `OppositionComponent` (lane acquisition + FSM + bolt + strip geometry)
- `Game/scenes/hazards/sentry_hazard.gd` — thin Actor-host shell (S2 skeleton; component wiring, param resolve, tells + S0 telemetry hook, test seams)
- `Game/scenes/hazards/sentry.tscn` — host scene (root `Sentry`, layer 16 / mask 2, `Lane`/`Body`/`Bolt` Polygon2D greybox + CircleShape2D r=14)
- `Game/data/oppositions/sentry.tres` — the def (card above; 11 params + mirrored `param_schema`, `trap_if_neutral` on `bolt_speed`)
- `Game/tests/test_sentry.gd` + `Game/tests/test_sentry.tscn` — headless scene test (12 cases, see below)
- `*.gd.uid` files for the three new scripts (generated by `--import`)

## Checks run

- [x] `godot --headless --path Game --import` clean (no parse errors)
- [x] `godot --headless --path Game --script res://tools/ci_smoke_test.gd` → SMOKE OK
- [x] `res://tests/test_sentry.tscn` → **U2b OK** (exit 0), covering the DoD:
  (1) def card + params↔DEFAULTS mirror + per-def bijection (count-agnostic — no
  global def-count assert) + host contract + Lane/Body/Bolt triangulate >0;
  (2) all-off gate — `&"sentry"` in no default lever/preset/bands-1-3 deck, all-off
  fp **e943ac9c8bc1 byte-identical**; (3) lane geometry: `lane_dir_deg=90` → DOWN,
  derived pick = the single clear octant (second-tick latch, ≥2-frame await), all-clear
  tie-break → `CANDIDATES[0]`, forced-heading-into-wall latches a short `_lane_len_eff`
  with the strip drawn at it and no trigger beyond it; (4) windup lead honored — one
  `&"telegraph"`, WINDUP holds ~`windup_s`, **zero contact + no visible bolt before
  FIRE**, S0 vocabulary only; (5) bolt kill `kills`-gated — death channels exactly once
  (BUG6), `kills=false` → contact row only, run survives; (6) wall stops the bolt —
  zero hits behind cover, bolt final pos at the wall face, no pierce; (7) LOS suppresses
  windup; latched lane never re-derives; clearing the wall releases it; (8) cooldown
  bar `cooldown_s ≥ (lane_width + 2·PLAYER_R)/200` off the authored def (1.2 ≥ 0.28) +
  behavioral crossable gap (no hit, no windup inside the gap); (9) throw-kill disables
  permanently; host in `"hazard"` group in every observed state; (10) body never
  contact-lethal + `bolt_speed 0` trap-neutral cycles harmlessly; (11) deterministic
  placement via the REAL `EncounterBuilder`+`SpawnService` — same band twice → identical
  cells, `per_band_cap=5` binds, `min_band=4` refuses depth-3; (12) RNG-audit (no
  global-RNG substring) + structural audit (LaneWatch never touches group membership or
  collision_layer).
- [x] Regression net, all green sequentially: `test_opposition_def_schema` (now 10 defs
  in this worktree — dir-scan, count-agnostic), `test_def_menu_coverage` (menu section
  auto-appears from `param_schema` — DoD 3), `test_config_menu` (91/91),
  `test_ambusher`, `test_burrower`, `test_charger`, `test_band_pipeline_parity`
  (fp e943ac9c8bc1 pinned), `test_encounter_builder`, `test_spawn_service`,
  `test_new_hazard_spawn`.
- [x] Definition of done met: "all-off fp unmoved; bijection green; `test_sentry`
  (windup lead honored — bolt never fires before the authored flash; bolt kill
  `kills`-gated; bolt stopped by a wall/cover cell; cooldown gap crossable at authored
  speeds; lane detection geometry from params; throw-kill disables; deterministic);
  menu section auto-appears; worklog + cost ledger + commit SHA + deviations."

## Bespoke-code cost ledger (the UG3 scalability evidence)

| Artefact | Kind | Actual | Predicted (spec §4) | In budget? |
|---|---|---|---|---|
| `data/oppositions/sentry.tres` | data | 1 resource (11 params + 11 schema rows) | 1 | Yes — the point |
| `components/lane_watch.gd` | **new component** | **308 lines total / ~191 non-comment** | ~120-150 | **Yes — the ONE new component** (over prediction: the A1/A2 amendments added the tick-gate, the latched-length plumbing, getters, and the strip-geometry draw; still one component, shared stack untouched) |
| `sentry_hazard.gd` | host shell | 208 total / ~118 non-comment | ~110-140 | Expected honest per-def cost (S6a Wave-4 rule) |
| `sentry.tscn` | scene | 1 (host + 3 polygons + collision) | 1 | Expected |
| `tests/test_sentry.gd` + `.tscn` | test | 775 + 7 lines | ~450-550 | Excluded from the ledger (test code) |
| **Edits to shared/reused files** | — | **ZERO** | ZERO | **The headline: `charge_lane.gd`, `lethal_contact.gd`, `throw_interaction.gd`, `telegraph_fsm.gd`, `thrown_item.gd`, `encounter_builder.gd`, `spawn_service.gd`, `event_bus.gd`, `run_config.gd`, `config_strings.csv` all untouched** |

## `CFG_FIELD_SENTRY_*` gloss rows for the orchestrator's Wave-1 integration commit (amendment 11 — NOT applied here)

| CSV key | suggested gloss |
|---|---|
| `CFG_FIELD_SENTRY_BASE_COUNT` | Sentries per band (builder spawn card). |
| `CFG_FIELD_SENTRY_COUNT_PER_DEPTH` | Extra sentries per depth index (builder spawn card). |
| `CFG_FIELD_SENTRY_WINDUP_S` | Lock-on flash lead before the bolt fires — the fairness line (bolt never fires earlier). |
| `CFG_FIELD_SENTRY_COOLDOWN_S` | Post-shot gap where it cannot fire — the crossable window. |
| `CFG_FIELD_SENTRY_BOLT_SPEED` | Bolt travel speed down the locked lane (0 = no bolt). |
| `CFG_FIELD_SENTRY_LANE_LENGTH` | Lane strip length = bolt max travel = sightline-derive cap. |
| `CFG_FIELD_SENTRY_LANE_WIDTH` | Lethal corridor width (and the drawn strip width). |
| `CFG_FIELD_SENTRY_LANE_ALWAYS_VISIBLE` | ON: the lane strip is always drawn (route puzzle); OFF: revealed on windup. |
| `CFG_FIELD_SENTRY_FIRE_ON_BODY_EDGE` | OFF: fires when the player's centre enters the strip (forgiving); ON: any body edge. |
| `CFG_FIELD_SENTRY_LANE_DIR_DEG` | -1: derive the longest clear sightline at spawn; >= 0: force this heading (degrees). |
| `CFG_FIELD_SENTRY_KILLS` | Bolt contact is fatal (OFF: contact telemetry only). |

## Design deviations

**none.** Built to the U2b spec's `Resolved Decisions (Phase 3)` (A1 second-tick
acquisition, A2 latched `_lane_len_eff`, A3 latch lifecycle, A4 spawn card as amended by
breakdown amendment 1 → `per_band_cap 5`), OQ10 component-owned bolt, OQ-2 8-octant
derive + override, OQ-6 `per_room_cap 1`, OQ-7 stop-on-first-hit. The DR-4 fun-bundle
defaults (always-visible lane, centre-crossing, permanent throw-disable) ship as
recommended pending the Director's disposition — all three are data/knob-cheap to flip.
Two notes for the close-out sweep (not deviations): (a) `lane_watch.gd` landed at ~191
non-comment lines vs the spec's ~120-150 prediction (itemised in the ledger above — the
A1/A2 hardening is the delta; still one component, zero shared edits); (b)
`display_name = "Gate-Sentry"` is the spec's placeholder — U3/Director ratify the
fiction name; the mechanical id `&"sentry"` is stable regardless.

## Handoffs / follow-ups

- **Orchestrator @ Wave-1 merge:** apply the 11 `CFG_FIELD_SENTRY_*` rows above to
  `config_strings.csv` in the single integration commit (amendment 11), alongside U2a's.
- **U3 (band_four):** deck-pins against the shipped card `cost 2 / per_band_cap 5`
  (breakdown amendment 2: `lobber 5 / sentry 5 / charger 4 / bomb 6 = 20`); adds
  `&"sentry"` to `band_four.tres`'s deck. No U2b-side action.
- **UG2 watch-items (no code):** throw-disable piñata telemetry (throw-kills-per-sentry),
  short-lane latches (`_lane_len_eff` < ~4 cells) eyeball, marker-soup felt read.
- **Director (DR-4):** disposition the fun-bundle (always-visible / centre-crossing /
  permanent throw-disable) — shipped per recommendation, knob-flippable.
