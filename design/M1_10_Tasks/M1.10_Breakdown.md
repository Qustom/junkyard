# M1.10 — Second Generation Backend + Cave Band + Low-Sightline Oppositions — Breakdown

> **Iteration version of M1.x.** Authored from the M1.9 breakdown as a template, per CLAUDE.md
> "Version breakdown authoring — the four-phase process." M1.10 is **Director-directed content
> (2026-07-04) opened ahead of M1.9's SG2/SG3 verdict** (exactly as M1.9 was opened ahead of
> M1.8's HG3, and M1.8 ahead of M1.7's RG3); M1.9's Director playtest → SG2 → SG3 stay pending
> and non-blocking. The Director's directive: **add two more opposition types, and add a new
> band that tests the scalability of adding bands — using a different type of procedural
> generation**, sourced from `design/explorations/`.
>
> **Source explorations (the design input — read before any task):**
> - `design/explorations/exploration-20260625/procgen-bands/0-scalable-band-generation-system.md`
>   — its **Phase D**: the `CaveBackend` (cellular-automata caverns) as the second backend
>   behind the one `build_layout` interface; the "different type of procedural generation."
> - `design/explorations/exploration-20260625/procgen-bands/b3-organic-caverns.md` — the cave
>   archetype itself: bad sightlines as the play identity, the CA algorithm sketch, the four
>   open flags (backend cost, tile-vs-piece world, disorientation feel, carve determinism).
> - `design/explorations/exploration-20260625/hazards/1-ambusher.md` +
>   `.../hazards/1-burrower.md` — the two low-sightline oppositions the caverns doc itself
>   names as the archetype's "reason to exist." Alternates considered: `2-lobber.md`
>   (arcing shells — fits caves), `2-sentry.md` (lane denial — a poor cave fit, no straight
>   sightlines; belongs to a future socket band).

**The one thing this version must prove:** *the M1.9 architectures scale along their second
axis.* M1.9 proved "new content on the existing machinery is data" (new defs, new profile —
same socket backend). M1.10 proves the harder half of each claim: **a band built by a genuinely
different generator** (`CaveBackend`, cellular automata — no pieces, no sockets) slots in behind
the same `BandPipeline` interface with the entire downstream stack (grade, junk, encounter
population, materialisation contract, telemetry, debug menu) **reused unchanged**, and **two
more oppositions** land as def + one new component each, with zero engine rework. The measure of
success is the *cost ledger*: each task's worklog records how much bespoke code was actually
needed beyond the promised backend/component — that ledger IS the scalability answer TG3 judges.

## Scope guardrails

- **CaveBackend ONLY — no ScatterBackend, no WFC, no grammar.** One new backend is the
  experiment; two would blur the cost measurement. Open-field/scatter stays a future candidate
  (the exploration's own sequencing).
- **The three existing controls stay byte-identical at every wave boundary:** the all-off
  `RunConfig` fingerprint **`e943ac9c8bc1`**, the `band_greybox` profile fingerprint, and —
  new this version — the **`band_two` profile fingerprint** (M1.9's shipped content is now
  itself a control). Cave code must be reachable only through a `backend == "cave"` profile.
- **Determinism contract (non-negotiable):** the CA backend reseeds via the existing
  `RNG.seed_from(seed)` discipline; **all cave iteration is order-stable** (sorted flood
  regions, fixed cell-scan order — the exploration's called-out gotcha); connectivity repair is
  **deterministic CARVE** (the `ConnectivityGuarantee` stage's existing CARVE mode), never
  retry-loops on the global stream. Same `(profile + seed + rc)` key → same `fingerprint()`,
  byte-for-byte, forever.
- **No new opposition machinery.** Ambusher + Burrower ride the S0/S2/S3 stack exactly as
  shipped: `OppositionDef.tres` + `param_schema`, one new `OppositionComponent` each, spawned
  by `EncounterBuilder`/`SpawnService`, surfaced by S4's generated menu sections + per-def
  bijection net. Any needed edit to a *shared* host/component file is a flagged deviation with
  a designated single writer (see wave rules), not silent sprawl.
- **M1 lethality model holds:** both new oppositions are Actors with `*_kills`-gated
  `fail_run` (params-carried, per the Wave-4 M1.9 canon). No player HP pool, no Field/zone
  hazards, no damage-over-time.
- **91-knob model frozen; per-def net extends itself.** No new hand-authored `RunConfig`
  levers. New defs appear via the count-agnostic generated Oppositions tab; the per-def
  params↔schema bijection extends to 9 defs (7 shipped + 2 new). Coverage assertions never
  dropped.
- **No save-schema change.** The third portal is always present (M1.9 precedent, Director's
  standing directive); band choice stays run-state through the existing
  `dive_requested(band_id)` → `GameState` staging seam → `BAND_ROUTES`.
- **Placeholder art tint-only** (`palette_tint` tier-1, D-RAT-4 precedent). Cave floor/walls
  materialise as greybox geometry; PixelLab runs are Director-gated; pixel filter OFF;
  copy-not-move from `art_workshop/`.
- **Run/meta boundary + single-writer-per-`.gd`-file per wave** hold. `main_game.gd` has ONE
  designated writer this version (T1, Wave 2 — nobody else ever). `band_pipeline.gd` is T0's.
- **No M2 dependencies.** Depth signposting in caves ("deeper = farther home" is less
  eyeballable than a spine) ships as-is and is a TG2/TG3 watch-item, not a build task.

## What exists today (as-built anchors — cite these, not exploration sketches)

- **Backend seam, already declared:** `BandProfile.backend` enum carries `"cave"` as a declared
  value (`data/bands/band_profile.gd:26`) and `BandPipeline.generate` fail-louds on it at the
  Phase-A wiring guard (`systems/bandgen/band_pipeline.gd:45-48`) — the cave backend replaces
  that guard with dispatch. `BandProfile.validate()` has a socket-only branch
  (`band_profile.gd:82-95`) needing a cave branch.
- **Pipeline stage stack (T0 slots in as STAGE 1-2):** `BandPipeline.generate(profile, seed, rc)`
  → backend/archetype (today: `BandGenerator.new().generate` verbatim, `band_pipeline.gd:80`) →
  S5 flavor loop + `ConnectivityGuarantee` (CARVE/ASSERT modes, `:93-107`) → `DepthGrader.grade`
  + `compute_return_distance` (`:112-113`). Per-stage sub-seeds via `_stage_seed` (`:143`).
- **Downstream consumes FLOOR-cell 4-adjacency only:** `DepthGrader` (BFS depth), `JunkPlacer`
  (loot on `floor_cells`, `_JUNK_SALT` sub-stream), `EncounterBuilder.populate` (walks pieces in
  stable order, credit budget × `instability(band_depth)` = 1.0 + 0.15·(depth−1) → band 3 =
  **1.30**). None touch sockets — the exploration's reuse claim, now load-bearing.
- **Materialisation is piece-instance-based (T1's problem):** `_materialise_band`
  (`scenes/game/main_game.gd:818-836`) walks `band.pieces`, parents each authored
  `p.instance` (a ZonePiece scene), then `SocketSealer.seal_unused_sockets` caps unmated
  sockets. A cave band has **no authored instances and no sockets** — it needs a synthetic
  materialisation path (runtime-built greybox geometry + collision from `floor_cells`) and a
  void-sealing rule that doesn't assume sockets.
- **Band fingerprint:** `Band.fingerprint()` = ordered `piece_id@offset#mated` sha256. Synthetic
  cave pieces must feed it stably (T0 defines the synthetic piece identity).
- **Opposition stack (T2a/T2b ride it):** `SpawnService` (per-dive node, caps live-registry-
  derived, `ignore_entry_safety`/`ignore_room_cap` ctx escapes) + `EncounterBuilder` (deck lane
  def-major, J2 even-spread — FBM19 as-built) + `OppositionDef`/`DeckEntry` (def params <
  deck-entry < `rc.param_overrides`). Component set as-built
  (`scenes/hazards/components/`): `OppositionComponent` base, `ChaseMove`, `ChargeLane`,
  `PatrolMove`, `SpinMove`, `StraightBounceMove`, `ProximityTrigger`, `DepthLingerTrigger`,
  `TelegraphFSM`, `LethalContact` (has the externally-driven contact seam), `ThrowInteraction`
  (+ `thrown_item.gd`'s locked `resolve_throw_death` seam). Defs at `data/oppositions/` (7).
- **Routing:** `BAND_ROUTES` (`main_game.gd:49`) maps route key → profile id (`&"near"` →
  `band_greybox`, `&"band_two"` → `band_two`); `_resolve_band_profile` consume-on-read off
  `GameState.consume_pending_dive_band()`; hub portals emit `dive_requested(band_id)`
  (`departure_portal.gd`, per-instance `interactable_id` + route key override). Telemetry
  `band_id` = the ROUTE key, stamped on `run_started`.
- **Debug surface:** S4's generated Oppositions tab is count-agnostic (proven 4→6→7 defs);
  FBM19b's IN-DECK chips read deck membership per band. New defs need zero menu code.

---

## Task list (stable ids)

Prefix **T**; gate tasks **TG***. Per-task design docs (Phase 2) live beside this file as
`T<n>_*.md`.

### Wave 1 — Backend + the two oppositions  *(T0 ∥ T2a ∥ T2b — parallel worktrees, file-disjoint)*

#### T0 — CaveBackend: CA caverns generator + `CaveBandConfig` + pipeline backend dispatch
- **Milestone:** M1.10 (Wave 1)   **Assignee:** general-purpose   **BlockedBy:** none
- **Goal:** The second generation backend, per the exploration's Phase D. `CaveBandConfig`
  (Resource sibling to `BandGenConfig`: grid extents, `fill_pct`, `smooth_passes`,
  `min_region_cells`, nook/roughness knob) + a `CaveBackend` in `systems/bandgen/`:
  seeded fill → N smoothing passes (4-5 rule, pure integer CA) → keep-largest flood region →
  **deterministic carve** of secondary regions above threshold (sorted-region order) →
  entry anchor selection → emit **synthetic `PlacedPiece`(s)** carrying `floor_cells` so
  `Band`'s data shape (and `fingerprint()`) is backend-agnostic. Replace the pipeline's
  cave fail-loud with backend dispatch (`_backend_for`); add the `validate()` cave branch.
  **Touches only `systems/bandgen/` + `data/bands/*.gd` schema + its tests — NOT
  `main_game.gd`** (a test drives the pipeline directly; materialisation is T1's).
- **Definition of done:** new `test_cave_backend` (same seed+profile → same fp twice; diff
  seed → diff fp; single connected FLOOR component via the existing `is_band_connected` bar;
  region-keep + carve determinism across a seed matrix); `band_greybox` + `band_two`
  fingerprints byte-identical through the untouched socket path; all existing bandgen tests
  green; all-off fp `e943ac9c8bc1` unmoved; worklog (with the bespoke-code cost ledger) +
  commit SHA + deviations.

#### T2a — New opposition #1: **Ambusher** (def + one concealment component)
- **Milestone:** M1.10 (Wave 1)   **Assignee:** general-purpose + character-animator (tell/placeholder)   **BlockedBy:** none
- **Goal:** The loot-punisher from `1-ambusher.md`: HIDDEN (near-invisible, faint floor tell)
  → ARMED (player inside trigger radius: tell flash) → POUNCE (one fast lunge) → EXPOSED
  (vulnerable window, then re-hide or stay spent). Built as **`ambusher.tres` + ONE new
  component** (working name `Concealment` — visibility + collision/lethality gating by
  state), reusing `ProximityTrigger` (arm), `ChargeLane` or a trimmed lunge move (pounce),
  `TelegraphFSM` (tell), `LethalContact` (`kills`-gated), `ThrowInteraction` (throw-kill
  while exposed). Off by default; enters only `band_three`'s deck. Placement policy
  (near-loot bias) is a deck/params matter — no EncounterBuilder edit expected; if one is
  needed it is a flagged deviation.
- **Definition of done:** all-off fp unmoved; params↔schema bijection green (8 defs);
  `test_ambusher` (hidden = non-lethal + un-hittable; arm radius; tell precedes pounce by
  the authored lead; pounce kill gated by `kills`; exposed window throw-killable;
  deterministic placement); menu section auto-appears; worklog + cost ledger + commit SHA +
  deviations.

#### T2b — New opposition #2: **Burrower** (def + one phase-cycle component)
- **Milestone:** M1.10 (Wave 1)   **Assignee:** general-purpose + character-animator (decal/placeholder)   **BlockedBy:** none
- **Goal:** The rhythm area-denier from `1-burrower.md`: BURIED (body invisible +
  **un-hittable** — collision cleared; a ground decal tracks the player at `track_speed`,
  ignoring walls) → TELEGRAPH (decal pulse, authored lead) → SURFACED (visible, lethal,
  throw-killable, holds `surface_s`) → BURIED. Built as **`burrower.tres` + ONE new
  component** (working name `BurrowCycle` — the phase timer + collision-layer cycling +
  underground tracking), reusing `TelegraphFSM`, `LethalContact`, `ThrowInteraction`.
  Surfacing gives a dodge frame (the telegraph lead is the fairness line). Off by default;
  `band_three` deck only. Proves the component model handles **phased vulnerability** —
  a state axis no shipped def exercises.
- **Definition of done:** all-off fp unmoved; bijection green (9 defs); `test_burrower`
  (buried = throw passes through + contact non-lethal; telegraph lead honored — surface
  under player never kills inside the dodge frame; surfaced kill `kills`-gated + throw-
  killable; cycle timing from params; wall-crossing while buried); menu section
  auto-appears; worklog + cost ledger + commit SHA + deviations.

### Wave 2 — Materialisation  *(T1 alone — the version's sole `main_game.gd` writer)*

#### T1 — Cave materialisation + backend-agnostic sealing + downstream verify
- **Milestone:** M1.10 (Wave 2)   **Assignee:** general-purpose   **BlockedBy:** T0
- **Goal:** Make a generated cave *playable*: extend the materialisation path
  (`_materialise_band`) so synthetic pieces with no authored scene build **greybox floor
  visuals + wall collision at runtime from `floor_cells`** (tint-ready via `palette_tint`),
  and generalize void-sealing for a socketless band (perimeter wall from floor/void
  adjacency — whether inside the backend, a sealer sibling, or a synthetic-piece Geometry
  layer is the Phase-2 design's call; `SocketSealer` must cleanly no-op or be bypassed on
  cave bands, byte-identically for socket bands). Verify the whole downstream on cave
  output: DepthGrader anchors (entry spawn + gate at deepest), JunkPlacer scatter,
  EncounterBuilder placement, camera/player collision. Player-scale sanity (throats wide
  enough) via a min-corridor-width check or config floor.
- **Definition of done:** a cave-profile dive runs headlessly end-to-end (generate →
  materialise → junk + gate + spawns placed on FLOOR); socket-band materialisation
  byte-identical (fp + existing `test_rg1_m1*`/hub/routing suites green); all-off fp
  unmoved; `test_cave_materialise` (wall collision closes the play space — no
  floor-adjacent void gap; gate reachable); worklog + cost ledger + commit SHA + deviations.

### Wave 3 — The band as data  *(T3 — the scalability measurement itself)*

#### T3 — New band: `band_three.tres` (cave profile + deck + tint + identity)
- **Milestone:** M1.10 (Wave 3)   **Assignee:** game-director-designer (profile/deck/curve/pitches) + environment-artist (palette tint) + general-purpose (glue/tests)   **BlockedBy:** T0, T1, T2a, T2b
- **Goal:** Author the third band **as data**: `data/bands/band_three.tres` — `backend =
  "cave"`, a tuned `CaveBandConfig` (chamber-y, nook-rich, per the b3 feel), `band_depth = 3`
  (→ instability 1.30 credit budget), an `opposition_deck` = Ambusher + Burrower (the
  low-sightline natives) + a supporting draw from the existing roster at `min_band`-gated
  `DeckEntry` weights, its own depth/reward curve, `palette_tint` (tier-1, direction set by
  the identity pitch), and **2–3 name/fiction pitches to the Director**. Flavors: start
  EMPTY (the cave IS the flavor; `SetPieceInject`/`WearDecay` were built against socket
  machinery — wiring them to caves is out of scope unless free). **This task's worklog is
  the headline scalability ledger:** hours + lines of bespoke (non-data) code needed to add
  band 3, vs what M1.9's band_two cost.
- **Definition of done:** `band_three` generates deterministically (same seed → same fp,
  twice; connectivity green); deck spawns via the builder within caps at the 1.30 budget;
  `band_greybox` + `band_two` fingerprints untouched; headless profile-load contract test
  (id, backend, deck ids resolve, validate() clean); worklog + ledger + commit SHA +
  deviations.

### Wave 4 — Reachability  *(T4 — hub/routing, small, sequential)*

#### T4 — Third hub portal + `band_three` routing
- **Milestone:** M1.10 (Wave 4)   **Assignee:** general-purpose   **BlockedBy:** T3
- **Goal:** Third `DeparturePortal` on the hub (visually distinct tint per the ratified band
  identity; placed per the iso hub's y-sort + clear-lane rules;
  `interactable_id=&"portal_band_three"`; prompt names the band), route key `&"band_three"`
  added to `BAND_ROUTES` → `band_three`; existing two portals byte-identical. `band_id`
  stamping is already generic — verify the new key lands on `run_started` rows. Always
  present; no save change.
- **Definition of done:** both existing portal paths byte-identical (fp + hub contract
  test); new portal headless contract check (id, prompt, routes to `band_three` with its
  fp); `band_id == &"band_three"` on run rows; smoke + routing tests green; worklog +
  commit SHA + deviations.

### Wave 5 — Re-gate  *(standing playtest-gate steps)*

#### TG1 — M1.10 playtest build + verify + changelog + itch publish
- **Milestone:** M1.10 (Wave 5)   **Assignee:** qa-playtest-coordinator   **BlockedBy:** T0–T4 all Done
- Full verify matrix (import · smoke · all-off fp `e943ac9c8bc1` · 91-knob + 9-def bijection ·
  socket-band fingerprints (`band_greybox`, `band_two`) · cave determinism + connectivity ·
  all three portals route · preset parity · suite green); **update `changelog.txt`**
  (M1.9→M1.10 delta as a feature list: a third dive band with a new cavern generator behind
  its own portal, two new oppositions — per the changelog scope rule); **publish to itch**
  (`BUTLER=/mnt/c/wsl-libraries/butler/butler bash Game/tools/push_itch.sh`; Chrome/Edge,
  password-gated, network/human-gated).

#### TG2 — M1.10 telemetry / balance analysis
- **Milestone:** M1.10 (Wave 5)   **Assignee:** qa-playtest-coordinator   **BlockedBy:** TG1 + Director playtest
- Three-band comparison off the `band_id` stamp (run length, depth reached, end-causes,
  deaths-by-id); do the Ambusher tell + Burrower telegraph read as fair (deaths-per-first-
  encounter vs Wrecker/Splitter baselines); does the cave disorient productively or read as
  lost (time-to-gate vs band 2); 1.30 budget sanity; web perf with the cave's wall-collision
  geometry; `debug_dirty` runs filtered.

#### TG3 — M1.10 re-gate verdict (Director decides)
- **Milestone:** M1.10 (Wave 5)   **Assignee:** qa (assembles) → Director (decides)   **BlockedBy:** TG2
- Record go / iterate / pivot in `design/M1_10_Tasks/G4_findings_M1.10.md`. Watch-items:
  **the cost ledgers** (did backend #2 + oppositions #5/#6 hold the "content = data" price?);
  cave depth-signposting (lighting/cue assist next?); do Ambusher/Burrower enter shallower
  decks; ScatterBackend next or not; flavor stages on cave floor; hub portal row scaling
  (3 portals now — when does the hub need a band-select surface?).

---

## Dependency map

```
T0 (CaveBackend + config + dispatch) ──► T1 (materialisation; sole main_game writer) ──► T3 (band_three.tres) ──► T4 (portal + routing) ──► TG1 ──► TG2 ──► TG3
T2a (Ambusher: def + Concealment) ─────────────────────────────────────────────────────► T3 (deck references &"ambusher")
T2b (Burrower: def + BurrowCycle) ─────────────────────────────────────────────────────► T3 (deck references &"burrower")
```

## Wave / build order

1. **Wave 1:** T0 ∥ T2a ∥ T2b (parallel worktrees; T0 owns `systems/bandgen/` +
   `band_profile.gd`/`band_pipeline.gd`; T2a/T2b each own ONLY their new component + def +
   test + scene — any shared host/component edit is orchestrator-adjudicated first, per the
   Phase-3 seam assignments).
2. **Wave 2:** T1 alone (the version's only `main_game.gd` writer).
3. **Wave 3:** T3 (data authoring + contract tests; measures the cost).
4. **Wave 4:** T4 (hub + routing).
5. **Wave 5:** TG1 → Director playtest → TG2 → TG3.
6. **Wave close-out deviation sweep after every wave** (Director dispositions) before the next
   dispatch.

## Cross-cutting contracts (M1.10)

- **Three permanent controls, byte-identical at every wave boundary:** all-off `RunConfig` fp
  **`e943ac9c8bc1`**; `band_greybox` fingerprint; `band_two` fingerprint (both through the
  untouched socket path).
- **Cave determinism:** `(profile + seed + rc)` → same `fingerprint()` twice; order-stable
  iteration everywhere (sorted regions, fixed scan order); connectivity = deterministic CARVE;
  no cave code on any socket path.
- **Cost ledger discipline (new, version-defining):** every build task's worklog carries a
  **"Bespoke-code ledger"** — files + line counts of non-data, non-test code the task needed
  beyond its promised new backend/component — so TG3 can judge the scalability claim on
  evidence.
- Coverage discipline extended, never dropped: 91 frozen knobs asserted; per-def bijection at
  9 defs; generated menu sections + IN-DECK chips pick the new defs up with zero menu code.
- `setup(cfg, player, spawn_ctx)` handshake locked; params-carried `kills` gates; BUG6 latch
  semantics; primitives-only signals; def params < `DeckEntry` overrides < `rc.param_overrides`.
- **No save-schema change** anywhere in M1.10.
- Single-writer-per-file per wave (`main_game.gd`: T1 only; `band_pipeline.gd`/`band_profile.gd`:
  T0 only); parallel agents in `isolation: worktree`; verify branch topology before every merge;
  push + board mirror after every merge; one worklog per task naming the commit SHA + deviations.
- Placeholder art tint-only; pixel filter OFF; copy-not-move from `art_workshop/`; PixelLab
  Director-gated.

## Phase-3 cross-task amendments (orchestrator-adjudicated, 2026-07-05 — binding on the build waves)

The Phase-3 fresh-eyes pass resolved every per-doc open question (each doc's `Resolved Decisions
(Phase 3)` section is BINDING and overrides its body where they conflict). Cross-task contracts
adjudicated across the six designs:

1. **Sealing seam (T0↔T1, settled):** the **unedited `SocketSealer` is the single wall-writer
   for BOTH backends** — T0 emits no walls but guarantees *data* enclosure (forced grid border +
   CA walls); T1 builds Geometry-bearing runtime instances and writes **FLOOR tiles only**; the
   sealer then caps floor-facing void verbatim. No double-sealing; `socket_sealer.gd` is never
   edited. The cave+flavors fail-loud lives in the **`validate()` cave branch** (single
   location; the pipeline guard defers to it).
2. **Entry anchor contract (T0):** the entry chunk's anchor = the **west-most 2×2-open** floor
   cell, front-positioned as `floor_cells[0]` (the as-built spawn read), remaining cells
   (y,x)-sorted. T1's `_entry_spawn_position` reads it unchanged.
3. **Throat-width protocol (T0↔T1↔T3, reconciled):** NO speculative widening pass in Wave 1.
   `carve_width = 2` covers carved connectors; T0 ships the 2×2-open throat **test** (C10, on
   defaults); T1's M6 spawn→gate 2×2-open bar enforces it end-to-end; **T3 clones the
   certificate onto the authored band-three config** (+ re-pins `max_depth >= 4` and
   gate-reachability there). Any red bar = a **T0-side fix or T3 config tune — never a T1
   patch, never a weakened bar.**
4. **CaveBandConfig schema (T0↔T3):** T0's **integer-only** schema is canonical — T3's float
   `nook_roughness` is DROPPED; roughness = `wall_threshold` + `smooth_passes`. band_three's
   authored set: `56×56 · fill_pct 45 · smooth_passes 4 · wall_threshold 5 · min_region_cells
   24` + T0 defaults (`carve_width 2 · chunk_cells 8 · min_floor_cells 300 · max_attempts 8 ·
   cell_size_px 16`), authored at `data/bands/cave_config_band_three.tres`; T0 sanity-runs this
   set at integration. Chunk partition (content-hashed `cave_` piece ids, `max_depth >= 4` on
   defaults) is ratified — an unchunked cave would zero the depth economy.
5. **Concealment mechanism (T2a↔T2b, converged where it matters):** *pass-through* is
   `collision_layer = 0` in the hidden/buried phase (T2a's group-only gating amended — a hidden
   solid body leaks position and blocks the player); the `&"hazard"` **group** carries
   throwability semantics. The two components stay SEPARATE (`Concealment` = presentation +
   membership gate; `BurrowCycle` = phase FSM) — same idiom, no shared new file.
   `ChargeLane` adds the group at bind (`charge_lane.gd:104`), so T2a's conceal-LAST ordering is
   load-bearing and pinned by test.
6. **`ui/config/config_strings.csv` Wave-1 collision (T2a↔T2b):** NEITHER branch touches the
   CSV. Both gloss row sets (prefix **`CFG_FIELD_*`** — T2b's `CFG_GLOSS_*` prefix corrected)
   are applied by the orchestrator in ONE integration commit at the Wave-1 merge (the S6b/S4
   precedent).
7. **Desync salt (T2b):** `legacy_ctx` stamps `phase_salt` for `&"spike"` only (as-built) —
   burrower desync derives **positionally** (SpawnService sets position before `setup()`), ctx
   key kept as a harness override. No `encounter_builder.gd` edit.
8. **No global def-count asserts:** neither `test_ambusher` nor `test_burrower` hard-asserts a
   total def count (8 vs 9 collides at the parallel merge); the dir-scanning coverage net is
   the counter.
9. **Deck + economy corrections (T3, binding):** deterministic deck outcome at 31 credits
   (instability 1.30) = **ambusher 6 · burrower 3 · splitter 4 · bomb 1 = 14 spawns, budget
   exactly 0** (`DeckEntry` cannot override typed def fields — bomb is budget-bound to 1);
   junk **density re-based to ~1.0→1.3** (chunked caves have ~35–45 pieces vs band 2's ~16 —
   the proposed 2.3→2.8 would have flooded 2.5–3× band 2's loot); reward curve value 1.30→2.5,
   tier 3→5. `flavors = []` is MANDATORY (validate() fail-louds on cave flavors). TG2 compares
   **band-total value**, not item counts.
10. **Physics amendments (T2b, binding):** surfaced `kill_radius` 26→**34** (player mask 26
    collides with hazard layer 16 — sum-of-radii ~30 px made the surfaced body a safe-to-hug
    bollard; first lethal test runs on the surfacing frame); BURIED→TELEGRAPH gated by an
    in-component `intersect_point` (world mask 2) check so it never surfaces inside a wall.
11. **`main_game.gd` writer wording (supersedes the guardrail's "nobody else ever"):** T1 is
    the sole writer **through its wave**; T4 writes exactly the one `BAND_ROUTES` row + the hub
    portal instance in Wave 4 (per-wave single-writer, as §T4 always said).
12. **Portal/pitch color story (T3↔T4, reconciled):** glow tints MULTIPLY over the
    violet-dominant portal art (green caps at 0.33) — T3's per-pitch glow column is withdrawn;
    glow is T4's, constrained green–teal. Reconciled pairs: Pitch A "The Warren" → blue-violet
    band tint + **cave-teal glow `Color(0.30, 0.90, 0.65)`**; Pitch B "The Hollow" → greener
    teal + a retone flag; Pitch C "Paradox Deep" → no free hue (ember clones portal 2) —
    strengthening the strike against C. Portal 3 at **(110, -20)** (forward-staggered second
    rank; 90.6 px gaps; spawn→portal-2 transit crosses its interact rect — a TG1 eyeball item,
    inherent to any second-rank slot).
13. **TG1/TG2 riders:** TG1's verify matrix gains the T2b binding riders (kill_radius 34
    surfacing-frame test; wall-clear surfacing test) and T3's absolute golden fp pins for the
    `band_two` control (no direct-generator path exists through its flavor stages).

## Needs Director review (assembled 2026-07-05 — the design locks when these are dispositioned)

Every technical/design-merit question is closed above and in the per-doc `Resolved Decisions
(Phase 3)` sections. The following are the **vision / fun / tone / scope calls** — each carries
a recommendation; the Director dispositions each (the M1.9 D-RAT pattern):

- **DR-1 — Opposition pick** (breakdown OQ1): ship **Ambusher + Burrower** as oppositions #5/#6
  (cave-native pair; each proves a new component axis). *Rec: ratify.*
- **DR-2 — Ambusher fun-bundle** (T2a OQ-3/4/5/9 + T2b CT-2): pounce **fatal, `kills`-gated**
  (no HP pool this milestone; Wrecker fairness precedent); tell defaults 0.28 alpha / 0.45 s
  lead (soften via `tell_lead_s`, never alpha); **one-shot** with dormant `re_hide_s`;
  **mimic-a-pickup variant NOT in M1.10**; hidden = true pass-through (no invisible-solid
  "bump tell"). *Rec: accept in full.*
- **DR-3 — Burrower fun-bundle** (T2b Q1–Q6): fiction **"Sinkmaw"**; **static pop** on surface
  (the locked-decal fairness contract requires it); **exact, locked-at-telegraph decal**; tempo
  defaults as authored with `kill_radius = 34`; the buried-unhittable counter-lesson ships and
  is validated at TG2. *Rec: accept in full.*
- **DR-4 — Both new oppositions band-3-exclusive** in M1.10 (clean A/B at TG2; D-RAT-2
  precedent). *Rec: yes.*
- **DR-5 — Band 3 identity** (T3 D1): **Pitch A "The Warren"** (blue-violet tint, cave-teal
  portal glow, "grown, not built" fiction) vs B "The Hollow" (phosphor-green) vs C "Paradox
  Deep" (struck twice on glow math). *Rec: Pitch A.*
- **DR-6 — Band 3 tuning bundle** (T3 D2/D4/D5): deck `[ambusher 6 · burrower 3 · splitter 4 ·
  bomb 1]` at the locked **1.30 → 31-credit** budget (+14.8% credits over band 2); reward value
  1.30→2.5, tier 3→5, junk density ~1.0→1.3. *Rec: accept; TG2 judges the felt step.*
- **DR-7 — Cave gate placement** (T1 OQ-1): pinned gate **snaps to nearest floor** (gate-at-
  deepest would invert the extraction loop on the play preset). *Rec: snap; cheap post-TG1
  flip.*
- **DR-8 — Cave wall look** (T1 OQ-2): **1-tile sealer shell over darkness** (perf + void-read
  consistency with socket bands) vs solid WALL fill. *Rec: shell; judge on the TG1 build.*
- **DR-9 — Portal 3 composition** (T4 OQ-3): (110, -20) forward-staggered rank — geometry
  verified; the Director eyeballs the plaza composition + the transit-prompt caveat on the TG1
  build. *Rec: accept; band-4 mirror slot (-110, -20) remains; band 5 forces a band-select
  surface (TG3 watch-item).*

**The build phase may dispatch only after these are dispositioned and folded in (Phase 4).**

## Open questions (breakdown-level — CLOSED; kept for provenance. Phase 2 designs expanded, Phase 3 resolved; Director dispositions above)

1. **Which 2 oppositions?** Recommend **Ambusher + Burrower** — they are the pair the caverns
   exploration names as the archetype's reason to exist (both *need* bad sightlines to read as
   fair), and each proves a component-model axis nothing shipped exercises (concealment;
   phased vulnerability with collision cycling). Alternates: Lobber (cave-compatible; mostly
   reuses the bomb blast model — a weaker architecture proof), Sentry (needs straight lanes —
   wrong band). *Vision/fun — Director ratifies.*
2. **Band 3 identity** — name/fiction/palette direction (2–3 pitches from T3's design;
   deeper/alien per the GDD escalation, `band_depth=3`). *Tone — Director.*
3. **Cave materialisation approach** — runtime-built greybox geometry (ColorRect/Polygon2D
   walls per synthetic piece) vs a generated `TileMapLayer`. Phase 2 (T1) researches both and
   recommends; the b3 "tile-world vs piece-world" flag. *Technical — resolve on merit.*
4. **Void-sealing for socketless bands** — backend-emitted perimeter walls vs a geometry-keyed
   sealer sibling vs `SocketSealer` generalization. *Technical (T0/T1 seam) — resolve on merit,
   byte-safety for socket bands is the bar.*
5. **Synthetic-piece fingerprint identity** — how `floor_cells` feed `Band.fingerprint()`
   stably (hash of sorted cells vs region-anchored synthetic ids). *Technical — resolve.*
6. **Ambusher pounce lethality** — exploration recommends a nonfatal stagger, but M1 has no HP
   pool; recommend **`kills`-gated fatal pounce with a generous authored tell** (Wrecker
   fairness precedent), stagger deferred to an HP-bearing milestone. *Fun/fairness — Director.*
7. **Ambusher re-arm** — one-shot (spent after pounce) vs re-hide cycle. Exploration recommends
   one-shot for the first build; a re-hide period param can ship dormant. *Ratify.*
8. **Burrower tracking fairness** — exact-position decal (fair/trivial) vs vague zone
   (tense/feel-bad). Exploration recommends clear telegraph + authored `track_speed` slow
   enough to outwalk. *Fun/fairness — Director.*
9. **Deck exclusivity** — both new oppositions band-3-exclusive in M1.10 (clean A/B at TG2,
   mirroring D-RAT-2)? Recommend yes. *Director.*
10. **Cave flavors** — confirm flavors ship EMPTY on `band_three` (SetPieceInject/WearDecay are
    socket-built; the cave is its own flavor). *Scope — confirm.*
