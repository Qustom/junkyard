# M1.5 — Agency & Legibility · Breakdown (Phase 1)

**Iteration of:** M1.4 (Stakes, Variety & Legibility), playtested 2026-06-24 (post-Wave-5 build) → verdict **ITERATE**
(`design/M1_4_Tasks/G4_findings_M1.4.md` §RG3).
**Authored:** 2026-06-24, from `design/M1_4_Tasks/M1.4_Breakdown.md` as template (the four-phase process, `CLAUDE.md`).
**Status:** Phase 1 (this doc). Next: Phase 2 per-task design fan-out → Phase 3 fresh-eyes → Phase 4 wire-up → build.

---

## 1. The one thing M1.5 must prove

M1.4 gave the run stakes + variety, and Wave 5 made the loop hold (the spawn-kill is gone, the pursuer is visible, the
camera reads). But the playtest exposed the danger as **something you can only run from, never answer**, the pursuer as
an **incomprehensible always-on chaser**, and two **legibility bugs** that nag every run (the money readout is hidden
behind the inventory; the grab prompt is always on screen). **M1.5 gives the player agency against danger — you can
*highlight an inventory item and throw it* to kill a pursuer — makes the pursuer a comprehensible, room-bound threat (a
slow patrol that only chases inside its spawn room), and fixes the two legibility bugs — then re-gates for a possible
"go."**

## 2. Scope guardrails (what M1.5 is and is NOT)

- **Greybox + configurable-not-balanced**, exactly as M1.1–M1.4. The two new *levers* (throwing, spawn-room pursuer) are
  swept `RunConfig` knobs whose *values* are the Director's to sweep; the thrown item reuses the existing `JunkPickup`
  greybox look; the pursuer behaviour is a config branch on the existing greybox `HazardEntity`, not new AI/navmesh.
- **The all-off `RunConfig` default stays the permanent baseline control** (determinism fp=`e943ac9c8bc1`, telemetry
  comparability with M1.0→M1.4). Every new *lever* knob defaults **off/neutral** so an unconfigured run still reproduces
  M1.0 byte-for-byte; the fun values ship in `make_default_play_preset()`, never by changing the code-level all-off
  defaults. This is the load-bearing carried contract.
- **The two HUD/interaction bug-fixes (L3 money-text, L4 grab-prompt) are NOT knob-gated** — they are correctness fixes
  that change behaviour for everyone (like Wave-5's BUG7/BUG8), not new levers. They do **not** touch generation, so they
  do not move the fingerprint.
- **NOT** new bands/art, a generator change, a meta/economy change, real enemy AI, or a save-schema change. Throwing and
  the highlight selector are **pure run-state** (remove an item from `run_inventory`, spawn a transient projectile) — **no
  meta persistence, no save bump**. The pursuer change is a behaviour branch + run-state, no save change.
- `run_ended` arity stays locked; telemetry stays additive (`data` fields only). Config-marked telemetry continues; the
  re-gate compares M1.5 cohorts to the M1.0/M1.1/M1.2/M1.3/M1.4 baselines on the same metrics.

## 3. Task list (stable ids)

| id | title | what it delivers | role(s) |
|---|---|---|---|
| **L0** | Foundation: knob + signal pre-declare | Single-writer pass on `run_config.gd` + `event_bus.gd` + `config_menu.gd`: pre-declare **every** M1.5 lever knob (throw group, spawn-room-pursuer group, **plus the L5 K5 `*_kills` toggles** `hpp_kills`/`hbomb_kills`/`hspike_kills` default `true`) at off/neutral defaults, extend `to_flat_dict()`, pre-declare every new EventBus signal, add the CFG menu MANIFEST/SECTIONS/FIELD_RANGE rows + CSV stubs, bump the knob-count tests. Unblocks the parallel build (no two build tasks write the three shared files). | general-purpose |
| **L1** | Throwing mechanic | Inventory item is **highlightable** (a selector navigable **Q/E**, wraps, re-validates on inventory change, visible highlight on the cell); **Space** removes the highlighted item from `run_inventory` and spawns a **thrown projectile** that travels in the player's **facing** direction; **hits a pursuer → kills it + destroys the item**; **miss (wall / max-range / lifetime) → re-drops the item as a grabbable pickup** (`EventBus.junk_dropped`). Includes the **input remap** (`project.godot`): **F = grab/interact AND extract/descend**, **Q/E = highlight L/R**, **Space = throw** (Director-locked). Knob-gated (`throw_enabled` default off, preset on). | general-purpose + ui-ux (highlight selector) |
| **L2** | Spawn-room pursuer (#6) | The pursuing `HazardEntity` becomes a **room-bound slow patrol**: it patrols within its spawn room and **chases only while the player is in that room**; outside, it does not chase (Director-locked semantics: *slow patrol*, not despawn/idle-freeze). New `r1_*` behaviour knob(s) (default off = today's chase-everywhere); config-marked telemetry mark. | general-purpose + game-director-designer |
| **L3** | Money-text reposition (#8) | Move the run-haul money readout out from behind the bottom-right inventory to sit **below the dive timer** (top-right), right-aligned, legible. Pure HUD layout (`decision_hud.tscn`), update logic unchanged. Bug-fix (not knob-gated). | ui-ux |
| **L4** | Grab-prompt visibility fix (#9) | The grab/interaction prompt is visible **iff** a valid grabbable interactable is in focus/range. Root-cause the always-on bug in the interaction-detector focus/prompt flow and fix it (not a band-aid). Bug-fix (not knob-gated). | general-purpose |
| **L5** | K5 per-hazard `*_kills` toggles *(M1.4 Wave-5 "Addressed" carry-in)* | Add `hpp_kills` / `hbomb_kills` / `hspike_kills` toggles (default `true` = today's lethal behaviour) to the three K5 hazard entities, mirroring R1's `r1_catch_kills`, so a non-lethal preset is expressible — then **retire `_driven_default_preset()`** (the verify driver runs the real preset with the K5 kills off). Knobs pre-declared by L0; entity files (`pingpong_hazard.gd`/`bomb_hazard.gd`/`spike_hazard.gd`) are disjoint from all other M1.5 tasks. | general-purpose + game-director-designer |
| **RG1** | M1.5 playtest build + verify | Assemble + verify the M1.5 loop (preset boots, throw highlights+throws+kills+re-drops, pursuer patrols/room-bounds, money readable below timer, grab prompt context-correct, all-off fp byte-identical, config-marked telemetry clean); **publish to itch** via `tools/push_itch.sh`; update `changelog.txt`. | qa-playtest-coordinator |
| **RG2** | M1.5 telemetry analysis vs M1.0–M1.4 | Distributions per config side-by-side across all six versions; did agency (throw-kills) + the room-bound pursuer + the legibility fixes land? throw-kill / miss-redrop / pursuer-state counts. | qa-playtest-coordinator |
| **RG3** | M1.5 re-gate verdict (Director) | go → M2; iterate → M1.6; pivot → rework. Recorded in `design/M1_5_Tasks/G4_findings_M1.5.md`. | Director |

## 4. Dependency map

- **L0** is the foundation (lever knobs + signals + CFG rows) — L1 and L2 read its knobs. `BlockedBy: none`; lands first.
  L0 is **file-disjoint** from L3 (`decision_hud.*`) and L4 (interaction files), so all three Wave-1 tasks run parallel.
- **L1** `BlockedBy: L0` (reads throw knobs; emits throw signals L0 declares).
- **L2** `BlockedBy: L0` (reads the spawn-room knob; emits the pursuer-state/throw-kill signal L0 declares).
- **L3** `BlockedBy: none` (HUD layout only). **L4** `BlockedBy: none` (interaction logic only).
- **L5** `BlockedBy: L0` (reads the `*_kills` knobs L0 declares); entity files disjoint from L1/L2 → parallel in Wave 2.
- **`main_game.gd` seam (Phase-2 to confirm):** L1 likely touches `main_game.gd` (to parent the thrown projectile into the
  band container so it's cleared with the band). L2 *may* touch `main_game.gd` (to pass the pursuer its spawn-room bounds
  via `setup`/`spawn_ctx`) — **or may be containable entirely in `hazard_entity.gd`** if the room bounds are already
  available to the entity. If both touch `main_game.gd`, **single-writer** that file (sequence L1 → L2, or assign one
  owner). Phase 2 resolves whether L2 needs `main_game.gd` at all.
- **RG1** `BlockedBy: L0,L1,L2,L3,L4`. **RG2** `BlockedBy: RG1 + human playtest`. **RG3** `BlockedBy: RG2`.

## 5. Wave / build order

- **Wave 1 — Foundation + legibility fixes** (parallel worktrees, file-disjoint):
  - **L0** — single-writer on `run_config.gd` + `event_bus.gd` + `config_menu.gd` (all M1.5 lever knobs + new signals + CFG
    rows). **Nothing else writes those three files this milestone.**
  - **L3** — HUD money reposition (`decision_hud.*` only).
  - **L4** — grab-prompt fix (interaction-detector / pickup / interactable only).
- **Wave 2 — Agency & threat** (after Wave 1 on `main`): **L1** (throw) + **L2** (pursuer) + **L5** (K5 `*_kills`, disjoint
  entity files → parallel). Run L1/L2 parallel **only if** Phase 2 confirms they don't both write `main_game.gd`; otherwise
  **single-writer** that file (sequence L1 → L2). L1 also owns the `project.godot` `[input]` remap (no other M1.5 task
  touches input).
- **Wave 3 — Re-gate** (sequential): **RG1** build+verify (publish to itch + changelog) → **Director playtest** → **RG2**
  analysis → **RG3** verdict.

## 6. Cross-cutting contracts (carried from M1.1–M1.4)

- **Configurable lever knob + config-marked telemetry**, all-off default reproduces the prior baseline as the permanent
  control. Every new lever knob joins `RunConfig.to_flat_dict()` + the CFG coverage assertion; update
  `tests/test_run_config.gd` / `tests/test_config_menu.gd` knob counts (current **81** → new count L0 sets). The throw +
  pursuer-behaviour knobs default **off/neutral**; the all-off control's fingerprint stays byte-identical (`e943ac9c8bc1`).
- **The default play-preset is a separate artifact** (`make_default_play_preset()`), NOT the code-level all-off default.
  The M1.5 fun stack layers throw-on + spawn-room-pursuer-on atop the M1.4 preset.
- **Throw + highlight are pure run-state** — removing an item from `run_inventory` and the transient projectile never feed
  `fingerprint()` and never persist. A missed throw re-enters the world via the existing `EventBus.junk_dropped` →
  `JunkSpawner` re-spawn path (reuse, don't reinvent).
- **No save-schema change in M1.5** (no new persisted meta). If Phase 2 surfaces any persistence need, that's a flag to
  the Director, not a silent bump.
- **`run_ended` arity locked**; telemetry additive only (new `data` fields / new signals, never an arity change).
- **Single-writer-per-`.gd`-file** within a wave; **pre-declare every new EventBus signal on `main`** (L0 does this for the
  whole milestone up front). **Parallel agents in `isolation: worktree`**; verify each branch's topology before merge (the
  qa-agent `git switch` leak — orchestrator memory); push `main` after every merge; mirror task status to the board; run
  the wave close-out deviation sweep (Director dispositions) after every wave.

## 7. Open risks to resolve in Phase 2/3 (per-task designs author these; fresh-eyes/Director ratify)

- **Throw selector ownership & input routing (L1):** where does the highlighted-index state live (`inventory_panel.gd`?)
  and how does the throw action read it cleanly — direct reference from `main_game`/player, or an EventBus `throw_requested`
  signal? The remap binds **F to both `interact` and `extract`** — confirm a single F press at a gate doesn't fire *both*
  actions in a conflicting way (prefer extract at a gate); confirm no other consumer relied on Q/E/Space.
- **Thrown projectile shape & collision (L1):** Area2D vs CharacterBody2D; collision layers/masks to detect the **pursuer**
  (`hazard` layer) vs **walls** (`world`); how "kill the pursuer" despawns it (the R1 hazard has no health — `queue_free`);
  does a throw-kill of the R1 pursuer reuse `new_hazard_killed(&"pursuer",…)` or want its own signal? Max range vs lifetime
  for the miss → re-drop; where the projectile parents so it's cleared with the band.
- **Throw vs the K5 hazards (L1):** does Space-throw also kill the ping-pong / bomb / spike hazards, or only the R1
  pursuer? (Director feedback names "pursuer" specifically — confirm scope; recommend pursuer-only for RG1, others a
  later sweep.)
- **Spawn-room bounds for the pursuer (L2):** how does the entity know "its spawn room" — does `setup()`/`spawn_ctx`
  already carry the room/piece bounds (the K5 pattern), or must `main_game.gd` pass them (the `main_game.gd` seam above)?
  What is "patrol" precisely — pace between two points, random walk within bounds, or idle-pivot — at what speed relative
  to chase? Re-entry behaviour (player leaves then returns to the room): resume chase immediately?
- **Highlight visuals (L1):** how the highlight reads against the existing `inventory_cell` styling (border vs tint), the
  default highlight when items exist, and the empty-inventory case.
- **Knob set (L0 pre-declares):** the exact names/defaults for the throw group (`throw_enabled`, `throw_speed`,
  `throw_max_range`/`throw_lifetime_s`, maybe `throw_kills` scope) and the pursuer group (`r1_spawn_room_only` +
  patrol-speed/params) — Phase 2 defines, L0 freezes the list + the new knob-count, contiguous-prefix house style
  (`throw_`, `r1_`).

---

## Phase 3 Dispositions & Phase 4 Lock

_Pending. Filled after the Phase-2 per-task designs are written, fresh-eyes resolve their Open Questions, and the Director
dispositions the flagged fun/scope calls. Until then the design is NOT locked and no build wave begins (the four-phase
gate, `CLAUDE.md`)._
